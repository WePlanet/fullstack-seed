form(name="form" ng-submit="login(form)" novalidate)
  div.form-group
    label(for="email") Email Address
    input.form-control(type="email" name="email" ng-model="user.email" ng-trim="true" required email-validator autofocus)
  div.form-group
    label(for="password") Password
    input.form-control(type="password" name="password" ng-model="user.password" ng-minlength="6" required)
  div.form-group
    input#stayLogin(type="checkbox" name="stayLogin" ng-model="stayLogin")
    label(for="stayLogin") Stay login

  div
    p: code(ng-show="(form.email.$error.required || form.password.$error.required) && submitted")
      | Email address and password are required.
    p: code(ng-show="form.email.$error.email && submitted")
      | Invalid email address format.
    p: code(ng-show="form.password.$error.minlength && submitted")
      | Password should be longer than 6 charactors
    p: code(ng-show="errors.other && submitted" ng-bind="errors.other")

  div
    p(ng-show="try")
      span.fa.fa-spinner.fa-spin
      = ' Trying login...'

  div.form-group
    button.btn.btn-success(type="submit" ng-disabled="try") Login
  ul.list-unstyled
    li: a(href="/register") Register
    li: a(href="/forgot-password") Forgot password